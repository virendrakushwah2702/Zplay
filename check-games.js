const {createClient}=require("@supabase/supabase-js");require("dotenv").config({path:".env.local"});const sb=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);sb.from("games").select("title,html_content,created_at").order("created_at",{ascending:false}).limit(5).then(({data,error})=>{if(error){console.log("ERROR:",error.message);}else{data.forEach(g=>{console.log("---");console.log("TITLE:",g.title);console.log("HTML_LENGTH:",g.html_content?.length||0);console.log("FIRST_300:",g.html_content?.substring(0,300));});process.exit();}});